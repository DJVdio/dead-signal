# 待办（完成一项删一项）

## 2026-07-18 状态收敛（本轮封存）

本轮已落地并销单：探索队操控、cartesian 坐标/相机、点击搜刮与目的地危险度；实时潜行装备因子、噪音可视化、耗子 L3 黑暗隐匿与破隐先手；金手指帮锁门/外围围栏与四名敌方守备；神秘商人损坏狙击枪/维修指南互斥货架与维修配方链；断肢装备掉落与尸体容器/读档恢复；手电/火把电池耐久；劫掠者战斗火把、布鲁斯犬体、碎石堆 authored 彩蛋；家具/结构 hover 文案；沙发座位与读书/恢复乘算；已撤销的旧系统规划及入口残留；过时 requirements 与桌面垃圾文档。

本页旧批次段落保留作审计证据；同名“待做”文字不再是开放任务。当前开放项按归属归类：

- **GUI-only**：探索关/尸潮/夜防/家具/存档/断肢掉落/潜行噪音/门栏的运行时目视验收；包含寻路绕行、遮暗层次、进度条叠压、菜单与墙体外观。
- **GUI-only·新增美术验收**：探索环境物件密度/遮挡、12 类八方向逐帧角色动作、七类持械动作的蓄力/出手/收招节奏与真实 paper-doll 武器对齐、三张结局 CG 的文字可读性与裁切，统一留待实机验收。
- **authored/剧情待用户写稿或终审**：五关搜刮与调查文案、护士/道格布鲁斯/电台与结局 CG、精英丧尸样板、商人死亡台词、相位中文名。CG-A 军袭版六段旁白已接入，旧全灭 CG 死路由已退役，不再列为开放裁决。
- **Wiki/数值调参**：光照/嗅觉穿墙、尸潮与弹药/商人经济、夜防听力与潜行、狗饥饿/战斗/装备、地图危险度与探索时长、感染竞速与医疗、护甲支配现象；只改 Wiki/配置或实机校准，不凭旧报告改规则。
- **真正未完成代码**：当前无本轮已确认且不涉 authored 语义的遗漏；探索叙事点 `NarrativeTrigger.Click` 已接入右键命中→寻路→到位调查，路过不触发，并有 7 个 authored 点位与生产接线护栏。2026-07-21 已接入实机验收账本：半天经济快照、探索出返/负重/弹药/伤病、生产、遗体与关键设备、死亡及结局统一静默导出中文 CSV/Markdown，随存档续写；后续 GUI 验收无需手抄数值。

1. **数值平衡观察**（战报得出，仅调参不改规则）：~~钝器克甲不克人待补强~~ **已闭环（批次19）**——钝器提基础伤害已落地。
   - 🔴 **[已改记·核实 2026-07-17·todo-sweeper] 本条原记的钝器数值与胜率整组作废**，别再拿它当基线：
     - 原记的武器数值已被用户后续手改覆盖，不能继续作为当前配置说明；当前武器数值以 Wiki 配置表为准。
     - 原记的胜率无法复现，且与其他历史报告冲突；若要重新声称"钝器补强闭环"，必须重跑 Sim 落一组带 commit/日期的新数，不能复活旧数字。
     - ⇒ **要再声称"钝器补强闭环"，必须先重跑 Sim 落一组带 commit/日期的新数**，别复活上面任何一个数字。
   - 余下待办：双持手枪对无甲 0.7~5s 速杀，接入误差角弹道后复测。
2. ~~后续开发候选（未排期）：防御战 tick 集成、昼夜循环/世界地图骨架、聚餐系统~~ **[已销单·核实 2026-07-17·todo-sweeper] 整条已落地**：
   - **昼夜循环 / 世界地图骨架**：`godot/scripts/GameClock.cs`（白天/黑夜两相位，内部流程节点状态机）+ `godot/scripts/WorldMapPanel.cs` 均已建成。
   - **聚餐系统**：`DawnMeal`/`DuskMeal` 两个边界流程已在 `GameClock.cs:180`/`:218`（同属强制暂停模态、冻结世界）；`CampMain.cs:4579` `EnterDuskMeal`、`:4582` 聚餐结算、`:4715` 吃饭动画全接线；头顶气泡走 `meal_bubbles.json`（见第 11 项①）。
   - **防御战 tick 集成**：已落地（见第 3 项）。
   - **饥饿多级阶梯**：已落地——`HungerState.cs:30` 刻度 0=饿死…5=正常…6=吃撑，`:47` 每相位 −1、`:50` 吃一餐 +1（饿死为终态、进食不复活）。
3. ~~**守卫防御战**（后置未实现）~~ **[已销单·核实 2026-07-17·todo-sweeper] 三条待做全部已完成**，原记的"现状"整段是陈旧登记：
   - ~~缺守卫移动到岗位坐标的逻辑~~ → `CampMain.cs:5953` `StationGuards()` 已建（`:5205` 注释原话「D2：守卫走向各自岗位站位并挂上岗位加成」），岗位坐标读 `camp.json guardPosts`（`:974` `BuildGuardPosts`、`:990` `PlaceGuardPost`，实心岗位放置后重烘焙导航）。
   - ~~夜间也无丧尸袭营/生成~~ → `CampMain.cs:6045` 明写「**D3 袭营触发/生成**」，`_raidZombies`（:319）承载当前袭营波次，`TriggerRaid`/`SpawnCampZombies` 已接线（另有第四路 `_nightRaidActive` 夜间袭击者潜入，:329）。
   - ~~真正的防御战缺失 / 守卫防御战结算~~ → `CampMain.cs:74` 注释原话「袭营推进节流：**守卫目标分派 + 破防/胜负统计**……~0.15s 一次」⇒ 结算循环在跑。
   - **仍留的只有 GUI 目视**（headless 不能自证）：已并入第 12 项④，本条不再单列。
4. ~~**食物再生来源（农业 / 产食配方）**~~ **[已销单·核实 2026-07-17·todo-sweeper] 已建成三条产食路径**，原记「当前营地食物只减不增（消耗 + 搜刮），缺可持续产食路径」= 陈旧：
   - **营地菜园（种植）**：`godot/scripts/Farming.cs:242` `CropPlotSpec` / `:324` `CropPlotLogic`（用户拍板设定：16 颗上限、种薯＝土豆 1、周期 84 游戏小时＝3.5 昼夜、零维护、收获 50% 出 2 / 50% 出 1 ⇒ 净 +0.5/颗、永不亏种子）；配方 `Recipe.cs:643`（《农场主的一百个问题》解锁）；消费层已接线——`CampMain.cs:7556` `ExecuteCropPlotInteract`、`:9346` `CompletePlantJob`（走 `CraftingJob` 工时化）、`:9142` `ClearPlot`。
   - **捕鸟陷阱**：`Farming.cs:209` `CropPlotRuntime` 镜像形态；`CampMain.cs:5130` `ResolveBirdTrapsForPhase()` 同频掷点（注释标 [T75]「此前整条未接，鸟从来出不来」＝已修）。
   - **圈套陷阱**：`Recipe.cs:547` 注释原话「营地里**唯一不用出门、不担风险**的食物来源」。
   - **仍留**：以上数值全属"拟定待调"，归 param-calibration（第 14 项），本条不再单列。
6. **货币（白银）经济量级待用户设计**（系统与投放已落地，数值全 draft）：当前开局 40.00 银、10 处世界固定掉落合计 80.00 银、不卖物品的固定总上限 120.00 银；2026-07-21 已修复后期 7 处掉落漏做“整银→分”转换的 100 倍单位错误，并加全图护栏。当前只待拍板：①这组量级是否合适；②《木匠入门》30 银与互斥货物 60/25 银的价格；③商人 NightAct 夜访、DawnMeal 离场的窗口；④售罄后是否补货（当前不补）；⑤交易价率（当前卖出 60% / 买入 100%）；⑥商人死亡 authored 台词终审；⑦材料开工即扣后的失败/取消返还口径。机器账本见 `docs/research/2026-07-21-campaign-economy.md`。
7. **尸潮时限系统收尾**（规则见设计文档 §5；D1~D4 已落地）：①**数值校准**——DeadlineDay（当前 40 draft，已外置到 `godot/data/config/horde.json`）与瞭望台接触时点，以机器账本 + 实机校准；②**D3 尸潮不可胜**——第 40 天进入非可玩尸潮屠营 CG + 随机一名半残幸存者南逃 + 峡谷坏结局谢幕，旧“无限围攻全员战死”只作遗留、运行时不可达；③**D4 已完成**——当前唯一主线关键物是广播台发出设备，固定发现点 `RadioMainline.TransmitterDiscoveryId`，没有“维修材料”随机门槛；④主题互文台词仍由用户手写；⑤望远镜、HUD 倒计时、到期屠营与南逃序列只剩 GUI 目视验收。D2 临期软预警明确不做。
8. **电台主线 + 结局矩阵实装（用户总纲/三拍板已给，authored 细节待用户设计）**（规则见设计文档 §3"结局矩阵"+"电台分层机制前提" / §10.4）：
   - **电台主线与三条结局链已实装**：收听、广播台、发出设备定点投放、状态机、电台抉择、军袭坏结局、尸潮坏结局、三问通过后的举家南逃 WIN 均有运行时消费与测试；剩余仅 authored 文案/占位美术终审及 GUI 目视。
   - ①~~**回复电台→第 3 天军方白天来袭事件**（结局②）：军袭事件本体（战斗结算）未实装（`TryTriggerMilitaryRaid` 现 no-op，CG②路由+文本已就位、走预留分支）；触发时序、执行方式、留守/外出判定 待实装~~ 🔴 **[整条已销单·核实 2026-07-17·todo-sweeper 独立复核 + sweep-story + sweep-camp] 军袭本体已全实装，且这条记的路由本身是已被推翻的旧设计**：
     - **「`TryTriggerMilitaryRaid` 现 no-op」= 错的**：它有完整方法体（`CampMain.cs:7088`），链路＝`TryFireMilitaryRaidHook`（一次性门控）→ `SouthEscapeEnding.SelectEscapee`（随机存活者，掷点走可注入源）→ `BeginSouthEscapeEnding(escapee, SouthEscapeTrigger.MilitaryRaid)`；**调用点 `CampMain.cs:5173`**（每日黎明，返回 true 则跳过本次黎明聚餐、序列接管画面）。
     - **「触发时序、执行方式」= 已实装**：回复日 + 2 的白天到期（`RadioMainline.MilitaryRaidDelayDays`，已外置 `military.json`）；执行＝CG-A 屠营冻结脚本演出（`PlayMassacreCinematic`）→ 南逃走廊玩家操作段（`LoadEscapeCorridor`）→ CG-B `SouthEscapeFarewell`（峡谷前大桥未落、两哨兵冷眼 → 黑屏谢幕）。
     - 🔴 **「CG②路由+文本已就位、走预留分支」= 描述的是已推翻的旧设计**：军袭**不经全灭判定**，走**强制终局南逃谢幕序列**（`BeginSouthEscapeEnding` 序列内先置 `_gameOver`，末尾直接 `EndingPanel.Show`）。权威注明在 `CampMain.cs:374-375`；`_militaryRaidWipeContext` **恒 false**（grep 全库：仅声明 `:376` + 读 `:3271`，**零置位点**）⇒ `EndingKind.MilitaryWipe` 与 `EndingCg.ForGameOver` 的该分支**运行时永不可达**，仅为遗留签名/测试保留（`EndingCg.cs:34`/`:45`/`:52`，`CampMain.cs:3268-3271` 另有一处同义注明）。**[状态·2026-07-17 已完成] 用户已拍板退役这条死路由，`sweep-story` 已执行完毕**（`3430 Passed/0 Failed`）——已删枚举 `EndingKind.MilitaryWipe` / `MilitaryWipeTitle` / `_militaryRaidWipeContext` / 2 条路由测试，🔴 **authored 文案与别名全部保留**（`EndingCg.cs:99` 的 `MilitaryWipe` 别名指向同一份 CG-A 屠营文本，字符串字面量 MD5 逐字节未变）⇒ **退役的是死路由，不是结局②的内容**。这几个符号现已消失，上面的 `file:line` 已失效——**请以"死路由已退役"为准，别照着行号找**。
     - 🔴 **「留守/外出判定 待实装」= 已随旧设计一并作废，不是遗留**（本条独立发现，非上游转述）：新设计是**无条件强制终局序列**——`EndingCg.cs:11` 原话「**不再是"全员在营才全灭"，而是无条件强制终局序列**」⇒ 谁在营、谁出门**根本不再参与判定**，这个待办**没有对象**。
     - ✅ **CG② 的内容仍在播**（屠杀 + 随机一名幸存者半残南逃 + 谢幕），作废的只是**"经全灭判定路由到 `MilitaryWipe` 分支"这个实现方式**——**别把这条读成"结局②被删了"**。
     - **仍留**：CG-A/CG-B 文案属用户手写剧情、待终审（见本项「文案待用户终审」）。
     **军方屠杀动机 = 留白定稿**（用户拍板"暂不需要动机"，CG②不解释；三候选案暂不采用、存档备查 drafts §1，用户日后可选一升正史）。**[核实 2026-07-17] 该留白在代码中仍然成立**（`EndingCg.cs:14` 原话「军方动机保持不解释（[SPEC-B11] 留白）」）。
   - ②**南逃生路线已实装**：`SouthTrial` 三问按 0/1/2 分计分，满 5 分通过后进入举家南逃 WIN；失败则南方永久回绝、退回持设备态，可改选军方或等尸潮。取得发出设备后时限冻结；三问失败会清除冻结、恢复第 40 天时限。剩余为题目/答案/旁白 authored 润色与实机演出验收。
   - ③**不回复电台→第 40 天尸潮**（结局①）已随尸潮时限系统落地（见第 7 项）。
   - **文案待用户终审**：军方循环救援广播文案、电台抉择（回复军方/呼叫南方）文案、结局 CG 正文——属用户手写剧情，以 radio-mainline 草稿 + 用户终审为准。（三结局 CG 初稿已备：drafts-authored.md §4；广播/抉择草稿见 RadioMainline.cs）
   - **知情入口无引导**（用户拍板·有意为之）：主线不给醒目指引，全靠玩家发现，与尸潮无预警一贯（story-review 发现③的叠加效应是已知刻意设计，非缺陷）。
   关联 §3 结局矩阵 / §5 尸潮时限 / radio-mainline（电台主线系统）/ WorldMapPanel（广播台进图）。
9. ~~探索关正式化专项~~ **[已销单·核实 2026-07-21]** 探索队操控、cartesian 玩法坐标 + faux-iso 显示/相机/输入反投影、右键点击物资搜刮（到位后与踏入共用逐件搜刮/可续搜）及 per-destination `DangerTier`/`enemyCount` 已落地，护栏见 `ExplorationInteractionTests`、`ExplorationCacheTests`、`ExplorationLevelSizeTests`；门后特殊丧尸开门激活与普通丧尸视野/噪音/靠近唤醒保持既有语义。世界材质、环境细节、营地道具、泛用/具名角色与布鲁斯 8 方向图集均已接入；装备纸娃娃已补齐剩余 22 武器 + 33 人类穿戴 + 5 布鲁斯装备（连同首批共覆盖 27 件正式武器及两种持光物）；角色动作状态已接入真实运行时事件：行走、10 类武器攻击、受击、工作、站/坐读书、聚餐坐姿、睡眠/卧床/手术躺姿，身体与全部穿戴/持械共用动作变换；战斗/交互反馈已补枪口焰与抛壳、弓弩释放、四类可见弹道、挥击、甲/锐器/钝器/致命/墙面命中、死亡、拾取、开关门及工作扬尘，且 cartesian 弹体使用独立可见 faux-iso 伴随节点；音频层已接 Music/SFX/Ambience 总线和空间事件，并正式资产化为 7 首自适应音乐、5 条环境循环及 22 类各 3 变体音效（共 78 个 WAV master），旧程序合成只作缺失兜底；上述视听表现仍留统一 GUI/实机验收。`NarrativeTrigger.Click` 已是仅点击后寻路到位触发、路过不触发；警察局 3 间开放侧房是否补门属关卡/authored 决定。
   - **装备与动作可视化（已落地）**：paper-doll 绘制层、真实左右手/双手/双持/手持光源、27 把正式武器、33 件人类穿戴、5 件布鲁斯狗装备及改装武器基础外观回落均已接线；`ActorAnimationCatalog` 钉死全武器动作分类和状态优先级，攻击只在实际出手后播放。剩余工作仅为 GUI 八方向锚点、动作幅度与遮挡关系的统一实机验收。
10. **光照与视野系统收尾**（批次4落地，规则见设计文档 §5"光照与视野系统"）：①~~电池 / 耐久系统待设计~~ **已落地（光源表已接线）**——手电默认电池 1440 游戏秒、火把耐久 480 游戏秒；按时标消耗，耗尽停止照明/暴露但仍占手，存档保留余量；固定光源无限。时长拟定待 Wiki 调整；②**丧尸嗅觉兜底已拍板·待调参**——用户已拍板"加"：丧尸在锥形视觉外附短程全向嗅觉（进半径即被闻到），补偿"摸黑贴脸从侧后白嫖"。待定：半径（拟定 60~80px）、**是否穿墙（默认不穿=同房间才触发，待确认）**，以实机 / Sim 校准。注：此为**丧尸侧的敌方感知兜底（嗅觉）**，与第 12 项②守卫警戒力的"听力轴"是**不同侧、不同感官**，二者不再合流为一套（见第 12 项②修订）；③**遮暗观感 GUI 目视验收**——遮暗渲染层次、视野外隐藏揭示、营地夜间开关的实机观感 headless 不能自证，需运行时目视；④**数值校准**——光照曲线（视距 / 锥角随 L 的映射，白天满档 vs 黑暗）、持光源暴露距离提升、三类光源强度 / 衰减、室内恒暗标记覆盖，均拟定待调，以实机 / Sim 校准。关联 `VisionLogic.cs`（vision-core）/ `ExplorationLevel.cs`+遮暗渲染组件（vision-render）/ `Zombie.cs`·`Raider.cs`（enemy-vision）/ 光源物品·建造（light-items）。
11. **道格与布鲁斯收尾**（批次5落地，规则见设计文档 §5"道格与布鲁斯"/§8"专属效果系统"）：①**入队时机已定稿·性格/台词待用户手写**——入队定稿=**南林村庄大调查点救援**（doug-village 实装：锁屋+丧尸围困+中距离布鲁斯吠叫引路+解救饿昏迷道格→回营入队，规则见 §5"道格与布鲁斯·入队"）；**人狗性格/对白/支线走向仍属用户手写剧情**（救援虚弱话语 draft 待细化；聚餐人狗日常/布鲁斯死亡/道格死亡后旁观气泡已落 meal_bubbles.json，走 bruce_present/bruce_dead/doug_dead 门控）；②**布鲁斯（狗）进食已拍板·数值待调**——定稿：吃 1 份食物=饥饿 +3、每次聚餐 −1、不上桌/不占坐席/不产气泡（规则见 §5"道格与布鲁斯·进食"）。待办：狗饥饿上限 / 挨饿惩罚形态数值校准；**DawnMeal / DuskMeal 各结算一次，即一天两次**；纳入 §4 聚餐分粮实装（狗单列 1 份）归 bruce-actor；③**数值校准**——升级阈值（**7 / 14 天共同存活**；🔴 **[已改记·核实 2026-07-17·todo-sweeper] 原记「5 / 12 天」= 陈旧**，真值见 `DougBruceBond.cs:49` `Level2Days = 7` / `:51` `Level3Days = 14`，改于 commit `c10c51e`、清单没跟）、3 级光环半径与作用对象（道格布鲁斯二者 vs 全营，主 agent 默认前者待用户确认）、站岗 75% 系数、各 +10%、布鲁斯撕咬伤害、闪避幅度、5 件狗装备护甲梯度，均拟定待调（狗装备数值以 §6「护甲表 v2」为准），以 Sim / 实机校准；**口袋狗衣负重已拍板 6kg（批次18），移出待调项**；**布鲁斯 solo 胜率已高出目标区间上限、待用户拍板**：🔴 **[已改记·核实 2026-07-17·todo-sweeper 承 sweep-combat-engine]** 原记「因腐皮锐防翻倍从 39% 降到 **34.4%**（2v1 仍 95.0%）」= **陈旧，且结论是反的**——真值 **56.4%**（`docs/research/2026-07-12-dog-calibration.md:8`/`:18` 已 committed 的数，sweep-combat-engine 重跑独立复现 56.6%，噪音级差异）。**报告 `:18` 自己就明写「solo 胜率 56.4%（目标 30~45%）」⇒ 已越过区间上限 45%，不是"偏低待回调"，而是"偏高"。** 🔴 **这条比数字错更毒**：原文会让人以为布鲁斯偏弱、该往上调，实际方向**完全相反**。**待用户拍板：回调闪避幅度（当前 0.45）／重标目标区间 —— 二者皆属数值决策，不自裁**；④**L2 修订待确认项**——道格 2 级原"1.25x 缠斗伤害"按用户 L2 改单被"狗装备制作"替换移除（主 agent 按原话直读），**用户可恢复叠加**；口袋狗衣的**狗负重是否吃移速惩罚**默认同人类规则、待确认；布鲁斯**闪避特殊能力实现形态**以 bruce-actor 最新 [HANDOFF] 为准（现引擎无闪避轴）；⑤**GUI 目视验收**——布鲁斯站岗 / 撕咬缠斗 / 高闪避表现、狗装备穿戴（身体+头 2 槽）、相依为命光环范围、视野角度加宽的实机观感，headless 不能自证，需运行时目视。关联 `DougBruceBond.cs`（doug-logic）/ 布鲁斯犬类 actor（bruce-actor）/ 狗装备配方（dog-gear）/ 光环·生产接线（synergy-wiring）/ `VisionLogic.cs` 缩放系数（vision-core）。
12. **夜防双班收尾**（批次6落地，规则见设计文档 §5"夜防对抗与双班制" / §7"生产工时化" / §6 服饰潜行值）：①**数值校准**——警戒力 / 潜行力合成与 roll 对抗公式、听力轴基值与距离衰减、疲劳 debuff 量、三档响应唤醒范围、1.5x 先手倍率、偷窃量、各配方 `WorkHours`、服饰潜行值，均拟定待调，以 Sim / 实机校准；②**守卫听力轴待设计**——警戒力的"听力"是玩家/守卫侧的新感知轴（基值+距离衰减），需用户拍板声音感知模型（脚步 / 战斗声、被听见判定）。**注：原拟与"丧尸听觉补偿"合流，现已解耦**——用户把丧尸兜底拍板为**嗅觉**（全向短程，见第 10 项②），与守卫的**听力**是不同侧、不同感官，各自独立设计，不再合并为一套；③**聚餐流程衔接已定稿（批次7）**——聚餐是全员短暂唤醒用餐的既定窗口（用餐后各自回睡）、且不被袭击打断（RaidAllowedIn 仅 NightAct），已落 §5，无遗留；④**GUI 目视验收**——三档响应拉人入战、未发现的 1.5x 先手 / 静默偷窃、晨间物资损失提示、生产工时在台推进 / 中断续做的实机观感，headless 不能自证，需运行时目视。关联 watch-contest（对抗纯逻辑）/ shift-sleep（双班硬日程+debuff）/ craft-worktime（工时化）/ night-response（运行时总接线）/ `VisionLogic.cs`·`ApparelSlots`·`CampStructure`·`CraftingService`。
13. **gameplay-review 评审建议待议（集合项，非阻塞）**——gameplay-review 的建议级发现，留待用户/后续统一议：①**主线拉力**（玩家推进主线的动机/牵引不足，与 story-review 电台主线缺口呼应）；②**夜载昼空**（夜晚活动密集、白天营地空转，节奏是否再平衡）；③**roster 权衡被钝化**（"带谁出门"的取舍在昼夜班硬日程下张力减弱——发现1已按"昼夜班战力分配"修订 §4，此处指更深的编队博弈调优）；④**守卫听力轴**（与第 12 项②同源，声音感知模型待设计）；⑤**读书量纲**（`ReadHours`/`WorkHours` 与 NightAct 实时时长的量纲校准）。均为方向性建议、非规则缺陷，待用户拍板是否采纳；详见 gameplay-review 报告。
14. **参数首轮校准已完成 + playtest 待验清单**（param-calibration 批次11）：全系统首轮 Sim/解析校准已跑——关键调整：布鲁斯撕咬 2-6/1.6s→**1-4/2.3s**、`Dog.DodgeChance` 0.5→**0.45**（solo 对丧尸约 39%、缠斗约 33s，达标）；持火把暴露 `MaxExposureBonus` 0.6→**0.7**（+41.7%，达标≥40%）；尸潮波次 10→12→14 逐波+2（"必死但戏剧性"成立，**参数留原值**）；狗食≈人 1/3、商人 60/100、匕首/步枪基线复核未破坏。**各批次 TODO 的"数值校准"子项中可 Sim 部分视为首轮已校**，余下为 **playtest 手感项**（生产工时松紧、经济量纲等），清单见 `scratchpad/param-calibration-report.md` + `docs/research/2026-07-12-{dog,vision,watch,endgame}-calibration.md`。~~夜防警戒 vs 潜行发现率 runtime ≈0 空间/接线 bug~~ **已修复（night-response）**：多带掷点 + 疲劳削视锥&听力项接通；param-calibration watchcal 复扫验收=裸营 48% / 满配 85% / 疲劳 Δ18pp 达标。
15. **调查点体量升级遗留（批次12，拟定待调）**：①**+46 物资搜刮点掉落 / 一句话叙事均 draft**——待用户验收优化（点位分布、掉落配比、叙事文案）；②**9 个叙事调查点文本 draft 待用户细化**（南林村庄祠堂祭台 / 卫生所登记簿 / 村口公告栏 / 水井童鞋、守林人小屋哥顿书桌 / 护林图、瞭望台护栏同心锁、广播台播音台 / 员工照片墙）；③~~**南林村庄游荡丧尸偏稀疏**——扩到 30+ 点后区域近翻倍，`VillageWanderZombieCount=4` 显稀疏，是否加量~~ **已随下方 [大图放大·impl-map-village] 处理**（画布 4200×2800、`VillageWanderZombieCount` 4→9，拟定待调）；④~~金手指帮"军械库"另立设计~~ **已闭环（批次12补）**——用户修正：金手指帮改**中型·以战斗为主**、已铺 11 处帮派储备物资点（白银首投放、禁食医、打过才拿），无需另立军械库。关联 `ExplorationCache.cs` / `NarrativeSpot.cs` / `TestExploration.cs`。
16. **批次13 五关 + 护士遗留（拟定待调 / 待用户）**：①**五地叙事 / 掉落 / 对话全 draft 待用户**——超市骗局接触/伏击/警告文案、医院、东部新村、加油站、南丁格尔的小药店的搜刮叙事 + 护士招募/接受/婉拒对话 + 留言板文案；**护士正式姓名"南丁格尔"为占位待用户改**；②**拟设定待用户否决**——东部新村"杂而薄"品类配比、加油站"燃油大户"身份（fuel 多点 + 地下储油间高价值）、医院丧尸巢密度（`HospitalZombieSpots` 14 只 band 12~16，是否偏高归 param-calibration）、药店小点危险度（走默认散布 5 只，是否调低归 param-calibration）；③**护士升级默认待确认**——升级轴=她本人手术台数，**成功/失败是否都计=默认"都计"待确认**、阈值 3 台→2 级 / 8 台→3 级**拟定待调**（三级特长**数值本身是用户原话、非拟定**：1 级手术基础点+15[她本人 30]、2 级全营感染 −15%、3 级全营基础点+5 且感染再 −10% 永续遗产）；④**GUI 目视验收**——超市骗局三分支、医院丧尸巢、护士招募 ChoicePanel 高度适配等实机观感。关联 `SupermarketAmbush.cs` / `NurseRecruit.cs` / `NightingalePerk`(`SurvivorPerks`) / `ExplorationCache.cs` / `NarrativeSpot.cs`。⑤**[中图放大·impl-map-mid1] 超市/加油站/东部新村抬到≈3天量级（数值全拟定待调）**：画布 2400×1600 → **3200×2200**（均匀 ×4/3，登记 `ExplorationLevelSize.Overrides`）；三张占位地台图各在 partial 补**真地形**——超市(货架阵错位实体+仓储/后巷带门房间+据点里屋)、加油站(便利店/修车棚带门建筑+地下储油间带门深房+前场抛锚车油桶垛)、东部新村(A/B/C/样板间四座带门排屋"一户户进门"+工地料垛脚手架实体+两座带门老宅院)；点位随纵深铺开、**数量维持 11/10/30 不新增**（受"不新编叙事 draft、叙事归用户"约束）；丧尸随分区重排、最深守高价值。护栏：`ExplorationLevelSizeTests`(三图移出回退 Theory 入 MidMaps 专测 3200×2200)，纯逻辑 `MarketHospitalCacheTests`/`NewVillageGasCacheTests` 计数不变恒绿。🔴 仍待实机校准（headless 不能自证）：ⓐ"≈3天"是否名副其实（工作量靠放大画布+逐个进门+料垛货架蛇形绕路结构上抬，真实天数只能实机测；若不足须补 loot 点=待用户写叙事 draft）；ⓑ蛇形动线/进门手感/搜刮点不落墙实机核；ⓒ**叙事调查点坐标仍钉旧 2400×1600 区**（`NarrativeSpot.cs` 非本单，已避开新墙保持开阔）、待用户定稿叙事时随画布重排。关联 `TestExploration.{Supermarket,GasStation,EastNewVillage}.cs` / `ExplorationLevelSize.cs`。⑥**[中图放大·impl-map-mid2·用户裁决 A] 仓库+斯图尔特放大≈3天量级；金手指维持原尺寸（放大另立单）**：三图分流——ⓐ**仓库**：画布 2400×1600 → **3200×2200**（登记 `ExplorationLevelSize.Overrides`），`TestExploration.HarvesterWarehouse.cs` 补真地形（南装卸区 + 中央货架/托盘堆垛实体通道 + 北阁楼最深带门房间 + 西办公/东油料库）、10 点随纵深铺开（数量维持不新增）；仓库入 `ExplorationLevelSizeTests` MidMaps 专测 3200×2200。ⓑ**斯图尔特（放大·逆缩放保噪音几何）**：画布 → **3200×2200**，🔴 用**绕庭院中心 (0.40,0.66) 逐轴逆缩放** `StuartManor.cs` 的 LevelW/H const（同步 3200×2200）+ `Posts`（X'=0.40+(X-0.40)×0.75, Y'=0.66+(Y-0.66)×8/11）⇒ 哨位间/庭院噪音的**像素距离逐字节不变**（`StuartManorTests` 噪音带 弓0/匕首1/手枪3/步枪6 + PostSpacing + TheHangedMen 恒绿），防御核心保持紧凑、放大出来的是周围田地/院外进路（更多步行/潜行绕哨空间）；`TestExploration.StuartManor.cs` 庄园地形/10 搜刮/2 采集随缩放后哨位重排、`NarrativeSpot.cs` 4 叙事点重排（门口吊尸 Y=1850 落入口侧满足 Y>LevelH×0.75、正文一字未动）、`ForageLogic` 两采集点重排（`ForageFarmingButcheryTests` 恒绿）；斯图入 MidMaps 专测。ⓒ**金手指帮根据地（维持原尺寸 2400×1600 零改动）**：均匀放大会把 `GoldfingerGang.Posts`（归一化）守备间距×4/3、改变"开一枪招几人"的 authored 噪音招怪（`docs/research/2026-07-14-goldfinger-calibration.md`）——用户裁 A 维持原状，守备/岗哨/11 储备点/2 尸体点全部原坐标零改动，仍在 `ExplorationLevelSizeTests` 回退默认用例。🔴 仍待实机校准（仓库/斯图·headless 不能自证）：ⓐ"≈3天"是否名副其实（工作量靠放大画布+进门+货架/堆垛/绕哨绕路结构上抬，真实天数只能实机测；点数受锁不新增，若不足须补 loot=待用户写叙事 draft）；ⓑ动线/绕哨潜行手感/搜刮点不落墙实机核。🔴 **[用户裁决 C·已结案·follow-on 已销单] 金手指帮据点维持 2400×1600 不放大**：本轮 18 图里金手指**维持不放大属用户明示裁决，不是遗漏**——别再当缺口捡起来重开。理由＝**authored 噪音红线**（「枪一响还是死」：弓/匕首叫醒 0 人、手枪 2 人、步枪 5 人）由「哨位布点的**绝对像素**距离 vs **全局固定**的武器噪音半径」直接决定，而放大必然改动它（实测：均匀放大 3200×2200 ⇒ 破甲锤 1→**0**、冲锋枪 4→**2**、步枪 5→**4**、狙击 7→**4**；2800×1900 ⇒ 冲锋枪 4→**3**、步枪 5→**4**、狙击 7→**5**）。该红线是 `GoldfingerGang.cs` [T57] 降难度拍板的**前提条件**（那段注释明写"噪音设计**一格没动**"），故维持原尺寸＝把红线原样保住、零测试风险。
   - **留档（免得日后重新调研）**：前一版 TODO 写的「**不能像斯图那样简单逆缩放，因招怪校准是绝对像素×守备密度的联合设定**」——**已实测证伪**（该结论针对旧 8 人表；用户改为 4 人后噪音招怪需按新布点重跑）。绕噪音源 (0.55, 0.40) 逐轴逆缩放（sx=2400/3200、sy=1600/2200）后，哨位到噪音源的旧 8 个距离与哨位两两间距**全部逐字节不变**，招怪表 0/0/0/0/1/2/4/5/7 原样保住，与斯图**数学同构**。⇒ **放大方案 A（逆缩放）技术上完全可行且风险低，但用户看过后仍选择维持不放大**（选项 C）。**结论是产品取舍，不是技术障碍**：A 只能换来院外进路/潜行空间，据点本体不会变大；若要据点内部真变大就得改 authored 招怪数（选项 B），用户不采用。
   - **可证明的约束（下次有人想放大前先读这条）**：招怪数 = 绝对距离 vs 全局固定噪音半径的比较，而 `NoiseRadius` 是 `Weapon` 字段、全表共享、约 10 处消费（改它会当场破 `StuartManorTests` 手枪→3/步枪→6 + 改变营地劫掠者行为）⇒ **要么守备阵列绝对尺寸不变（招怪免费保住），要么守备摊开（招怪必变），不可兼得。**
   - **已焊死的脱焊陷阱**：`GoldfingerCalibration.cs`（Sim）此前**硬编码** `2400, 1600` 调 `AlertedBy`，与真源 `ExplorationLevelSize` 之间**零保障** ⇒ 画布一改，游戏里招怪变了、报告还印旧数且无测试红。现改为读 `GoldfingerGang.LevelW/LevelH`（脱 Godot 副本，**由 `GoldfingerGangTests.画布尺寸与ExplorationLevelSize焊死_改了那边这边必须跟着改` 焊死在 `ExplorationLevelSize` 上**）；同时补 `枪一响还是死_弓与匕首叫醒零人而枪招来一片`（读真 `WeaponTable.NoiseRadius`，此前全项目**没有任何测试钉这条 authored 红线**）+ `越响叫醒的人越多`。🔴 **刻意不把 `ExplorationLevelSize` Link 进 `DeadSignal.Sim.csproj`**——「不链接 ⇒ 结算路径读不到 ⇒ 既有 Sim 战斗基线结构性零漂移」是项目纪律，取个画布尺寸不值得动摇它。**[已闭环·impl-church] 同形态的孪生陷阱 `StuartManor.cs` 的 `LevelW/LevelH` 副本亦已焊死**（照抄本焊法：`StuartManorTests.TheCanvasCopyIsWeldedToItsSingleSourceOfTruth` 断言副本 == `ExplorationLevelSize.SizeFor(斯图尔特家族庄园)`，跑在同时链接两者的 Combat.Tests 里 ⇒ Sim 的 csproj 一行未动、零漂移论证原样成立）。🔴 **斯图这处比金手指那处更毒**：金手指是"Sim 报告印旧数"，斯图是**护栏自己跟着副本一起漂**——运行时按真源铺哨位（`TestExploration.StuartManor.cs:152` `pos = Posts[i] × LevelW`，其 `LevelW` 即 `SizeFor` 值），而噪音护栏（弓0/匕首1/手枪3/步枪6、`PostSpacing`）**全部拿副本**把归一化 `Posts` 换算成像素，且改造前斯图这一侧**没有任何测试读过真源**（grep `SizeFor` 命中 0）⇒ 真源一改而副本不动，**游戏里手枪招怪 3→1、步枪 6→4、匕首 1→0**（实测：真源若改 4200×2800、副本留 3200×2200），authored 噪音带整条塌掉、「逐个清哨」的经济全变，**而那三条护栏一条都不会红**。先红已验（副本设错值 ⇒ 焊缝红 `Expected 3200(真源)/Actual 2400(副本)`）。零行为变更（两处本就一致 3200×2200，纯补锁；斯图既有护栏 + Forage + 画布登记 98/98 恒绿、全量 3427=基线3426+焊缝1）。关联 `TestExploration.{HarvesterWarehouse,StuartManor}.cs` / `StuartManor.cs` / `NarrativeSpot.cs` / `ExplorationLevelSize.cs` / `GoldfingerGang.cs` / `GoldfingerCalibration.cs` / `StuartManorTests.cs`。⑦**[中图放大·impl-map-mid3 + Phase2] 广播台放大；破败教堂/难民营地"维持不放大"的旧裁定已被 Phase1 推翻、二者均已放大**：（a）**广播台**已抬到≈3天量级——画布 2400×1600 → **3200×2200**（登记 `ExplorationLevelSize.Overrides`）；`TestExploration.BroadcastStation.cs` 从纯占位地台重写为**真广播站结构**（播音楼：北端机房大厅+中央脊廊挂 6 房；院内五外屋+侧间；东北天线塔基座剪影；墙走 `AddSolidWall` 挡路+断寻路+挡视线，房间复用 `ExplorationWalls.RoomOutlineWalls` 门宽 64）；物资 **10→16**（`ExplorationCache` 补控制室/天线机房/磁带库/车库/门卫室/锅炉房，电子/线材/燃油/碎铁为主·食物医疗仅门卫室·无枪），沿机房→脊廊→院子近→深铺开、16 点全在房内不落墙；🔴 authored 调查点（播音台/照片墙 `NarrativeSpot`）+发射机锚点 (1200,300) 文本与坐标一字未动。护栏：`ExplorationCacheTests`（广播台 10→16）、`ExplorationLevelSizeTests`（广播台移出回退 Theory 入 MidMaps 专测 3200×2200）恒绿。🔴 仍待实机校准（headless 不能自证）：ⓐ"≈3天"是否名副其实（工作量靠放大画布+进门+脊廊/院子绕路结构上抬，真实天数只能实机测）；ⓑ动线/进门手感/搜刮点不落墙实机核。（b）**破败教堂 = [Phase2·impl-church] 已放大到 3200×2200**（🔴 **旧裁定「authored 视野谜题关·紧凑体量为设计本体·维持不放大」已被 Phase1 推翻、作废**）：旧裁定的唯一依据是一条**固定像素**——「吓一跳」＝推开门时墓地 12 只须**同时挤进门洞站位的白昼锥（固定 300px/60°）**，墓地进深被锥半径钉死，一放大就散出锥外、惊吓当场没了。Phase1（探索期威胁模型）把它重定义成「**推开墓地边界那两扇门 ⇒ 门后整片冻结丧尸唤醒涌来**」（`ZombieActivation`·`RuinedChurch.GraveyardWakeDoors`）后，**触发绑的是门实体、与尺度无关** ⇒ 像素约束解绑 ⇒ 按用户口径「中→3天(3200×2200)」放大（数值拟定待调）。落地：画布登记 `ExplorationLevelSize.Overrides`；`RuinedChurch.cs` 几何重排（Left/Right/Top/Bottom 400/2800/150/1950、墓地线 500→**700**、屏风 780→**1100**、中轴 1100→**1500**、北耳门 1560→**2100**），放大只加**纵深与数量**——长椅 **5→6 排**、立柱 **3→4 根**、墓地进深 **364→534**；`TestExploration.RuinedChurch.cs` 四条分区地台随几何重排；12 处搜刮点随纵深/两翼铺开、**不新增 id**（受"新搜刮点要用户先写叙事 draft"约束 ⇒ `RuinedChurchTests` 四处登记表计数恒绿）；`NarrativeSpot.cs` 教堂两处证据（忏悔录 → 西侧告解亭内、血字 → 西侧廊）**只重排坐标、正文一字未动**。🔴 **门宽 72 / 中央走道 140 / 侧廊 64 / 长椅排距 90 / 立柱 70 一律不缩放**（按人体与丧尸直径 26 标定的物理常量，同 `ExplorationWalls.cs:231` 医院"门宽刻意不缩放"先例）——跟着画布 ×4/3 会把"挤得进窄槽""一次过得来几只"的战术含义全改掉。护栏：`RuinedChurchTests` 残留像素不变量随新画布重排（盲区眼点/spot、门前 0 可见、可达性 Level 改为**直接读 `ExplorationLevelSize.SizeFor`** ⇒ 与画布登记处同源焊死），并新增三条——中图档 3200×2200、**物理常量不缩放**、**几何整体落在登记画布内**；另把"门关着＝可见 0"从**可能靠距离蒙对**（新画布下部分丧尸本就在 300px 锥外＝假绿）改成**逐条钉遮挡**（每条视线都真撞在门板/边界墙上，与尺度无关）；`ExplorationLevelSizeTests` 教堂移出回退 Theory 入 MidMaps 专测 3200×2200。authored 语义零改动：墓地必关得死（不变量 A）/ 退路两个洞永远敞着（不变量 B）/ `GraveyardWakeDoors` 拓扑 / 「大量」≥10 只 / 教堂本体稀 ≤4 只 / 一把枪一发子弹都没有。🔴 仍待实机校准（headless 不能自证）：ⓐ「≈3天」是否名副其实（工作量靠放大画布+穿盲区+钻窄槽的结构上抬，真实天数只能实机测；点数受"不新增 id"锁，若不足须补 loot＝待用户写叙事 draft）；ⓑ**"开门→整片扑来"的惊吓强度与涌来层次感**——旧版靠"一眼看见一片"，新版靠"一群同时站起来朝你走"，墓地放大后主群离门更远/更散，扑咬抵达的节奏只能目视（若散了味，收紧 `GraveyardZombieSpots` 密度即可，坐标本就拟定待调）；ⓒ穿过中殿的盲区手感/搜刮点不落墙实机核。理由：核心机制吃**固定引擎常量的绝对像素**——教堂"吓一跳"＝推开门≥8 只丧尸同时进**白昼锥（固定 300px/60°）**——均匀缩放（医院 1.75× 那套）会当场破 `RuinedChurchTests`；且 loot 被自身测试封在中 band 10~16，到不了 3 天。它的"规模中"是**内容/危险**意义的中，不是物理尺寸。（c）**[SPEC-T60·Phase2·impl-refugee] 难民营地已放大到 3200×2200（≈3天量级，数值全拟定待调）—— 上面那条「维持不放大」的裁定，难民这半句已被 Phase1 推翻**：旧裁定的理由正是这一关吃**固定像素**——「开门跳脸」要求伏击丧尸贴在门后 ≤90px（< 室内暗视距 124px），否则门一开你还看不见它、跳脸就不成立 ⇒ **房间大不过那几十像素的窗口** ⇒ 整关钉死 2400×1600。Phase1（见上⑦、`ZombieActivation`）把威胁重定义成**绑门实体的门后特殊丧尸**（推开那扇房门才唤醒、一门一只）后，**触发与尺度无关 ⇒ 像素约束解绑 ⇒ 房间可以任意大**。落地：画布登记 `ExplorationLevelSize.Overrides[难民营地]=(3200,2200)`（用户口径「中→3天」）；`RefugeeCamp.cs` 几何**改成派生式**（Left/Top + 列宽 424 + 三排行高 520/520/480 ⇒ Right/Bottom/竖脊/六列左缘/GateX 全部算出来，不再手写字面量、消除两头对数）——三排六列 18 房 y(236..716 / 788..1308 / 1380..1900)、竖脊 x1564..1636（**脊长 1248→1664**，暗视距 124 连它 1/13 都不到）、营门 x1600、西豁口 y752；🔴 `AmbushDistance=90` 常量**已删**（Phase2 后没有任何东西读它，留着就是假事实源），10 只伏击站位改为**贴门 / 房中央 / 最深角落**三种混排（房间放大到 424×520 之后这三种手感不是一回事）；14 处搜刮点**不新增 id**（受"新搜刮点须用户先写叙事 draft"约束）、只随纵深压到背着门的那半边；`NarrativeSpot.cs` 两处环境叙事（字条墙 14 号房→(900,330)、尽头那间 18 号房→(2700,340)）随画布重排、**正文一字未动**；视觉地台改读新增的 `RefugeeCamp.Interior`（与几何**同一个事实源**）。🔴 **门宽 48 / 过道宽 72 刻意不缩放**（同 `ExplorationWalls.cs:231` 医院先例）——48 < 2×丧尸直径 52 的**战术漏斗仍然有效且要保住**：它从来不是 blocker，只是不再是"跳脸"的承载物。护栏：`RefugeeCampTests` 残留像素不变量随新画布重排（`InTheDark` 脊长改读 `RefugeeCamp.SpineLength`、`BehindEveryClosedDoor` 眼点、可达性 Level/ReturnZone 改读 `ExplorationLevelSize.SizeFor` **自动跟档**）+ 新增 `Phase2_TheCampWasEnlargedToTheMidTier_AndTheDoorwayWasNot`；`ExplorationLevelSizeTests` 难民移出回退 Theory 入 MidMaps 专测。🔴 仍待实机校准（headless 不能自证）：ⓐ"≈3天"是否名副其实（工作量靠放大画布+18 次推门+窄过道绕路结构上抬，真实天数只能实机测；点数受锁不新增，若不足须补 loot=待用户写叙事 draft）；ⓑ**放大后"开门唤醒"的实机手感**——房间大了，门后那只要从深处冲一段过来，是更吓人还是不够"跳脸"，只能目视；ⓒ恒暗关放大后手电取舍是否更疼、搜刮点/丧尸不落墙实机核。关联 `TestExploration.BroadcastStation.cs` / `ExplorationCache.cs` / `ExplorationLevelSize.cs` / `RuinedChurch.cs` / `RefugeeCamp.cs` / `TestExploration.RefugeeCamp.cs` / `NarrativeSpot.cs`。⑧**[小图适度放大·impl-map-small] 河边小屋/南丁格尔药店/守林人小屋/消防站抬到适度更大（数值全拟定待调）**：用户口径「地图尺寸都要更大一些」——小图给**适度放大**（不追 3 天量级）。画布 2400×1600 → **2800×1900**（~1.17×/1.19×，登记 `ExplorationLevelSize.Overrides`；键：河边小屋/消防站=`ExplorationCache.*Name`、守林人小屋=`WatchersCabinName`、药店=`NurseRecruit.DestinationName`"药店"字面量）；四张各在 partial 补**一点结构/绕路**（河边=河滩水面+沿岸栈道错位护栏+屋后菜窖棚+岸边杂物堆；药店=临街店面+后屋药房+**新增东侧库房**+阁楼四间；守林人=林间小屋屋中屋+**新增后院柴棚**+林下开阔地+两株老树；消防站=车库南半场+器材间/值班室两屋+后院训练场）；既有搜刮点随纵深铺开、**不新增搜刮 id**（受"不新编叙事 draft"约束 ⇒ 各图 *CacheTests 计数恒绿：河边5/药店5/守林5/消防5）。🔴 护栏：ⓐ**守林人小屋 `ForageLogic` 两采集点（林下腐叶/柴堆背阴）仅重排坐标、id 不动** ⇒ `ForageFarmingButcheryTests` 恒绿；ⓑ**药店走默认散布危险度未动**（5 只，是否调低仍归 param-calibration，见本项②）；ⓒ**消防站相对护栏**（`FireStationCacheTests` 低危掉落<河边/守林）不受影响——只改坐标未动 `ExplorationCache` 掉落；ⓓ`ExplorationLevelSizeTests` 四图移出回退 Theory 入新增 `SmallMaps_Enlarged_To_2800x1900` 专测。🔴 仍待实机校准（headless 不能自证）：ⓐ放大后手感/搜刮点不落墙/绕路动线只能目视；ⓑ叙事调查点坐标（`NarrativeSpot.cs` 非本单）仍钉旧 2400×1600 区、待用户定稿叙事时随画布重排；ⓒ哥顿上吊尸随后院老树移到后院深处（authored 后院口径不变、正文未动）。关联 `TestExploration.{RiversideCabin,Pharmacy,RangersCabin,FireStation}.cs` / `ExplorationLevelSize.cs`。**[小图收尾·impl-map-small] 城市之巅放大 + 下水道维持**：（a）**城市之巅瞭望观景台**只读判定＝**占位地台图·无 authored 固定像素不变量**（望远镜"瞭望正北尸潮"是 **flag 式发现点**——踏入置 `HordeSighted` 旗标 + 环境叙事，`LookoutSightingTests` 纯逻辑无画布耦合；`LookoutTelescopePosition` 只是可同步的演出锚点、非不变量）⇒ 同四小图**适度放大 2800×1900**：`TestExploration.CityRooftop.cs` 补天台观景平台结构（天台机房 + 瞭望员值班室两栋设备房 + 南侧游客服务楼 + 通风管道/水箱台/设备排实体绕路）、5 搜刮点随纵深铺开（`LookoutGiftShop/Vending/StaffLocker/MachineRoom/WardensRoom`，id 不新增 ⇒ `ExplorationCacheTests` 计数恒绿）；`LookoutTelescopePosition` 静态锚点从 `DefaultWidth/2`（1200）改为读 `ExplorationLevelSize.SizeFor(城市之巅).Width/2` ⇒ 自动同步到北缘正中 1400（`TestExploration.cs` 注释同步）；`ExplorationLevelSizeTests` 城市之巅移出回退 Theory 入 `SmallMaps_Enlarged_To_2800x1900` 专测。仍待实机校准：望远镜/护栏/搜刮点不落墙 headless 不能自证。（b）**下水道 = 无门·维持小不放大**（纯注释、零关卡代码改动）：下水道本就**无门关**，其核心＝拐角丧尸 + 室内恒暗视距 + "任一可行走点感知≤1"护栏（`SewerTests` 对尺寸敏感、并发≤1），均匀放大对这套拐角感知 + 黑暗约束**无益**（不像有门的图能靠开门激活解绑像素约束），故维持 2400×1600（本就在 `ExplorationLevelSizeTests` 回退用例里、自然全绿）。关联 `TestExploration.CityRooftop.cs` / `TestExploration.cs` / `ExplorationLevelSize.cs`。**[SPEC-T60·Phase2·impl-police] 警察局已适度放大 2800×1900（小图档，数值全拟定待调）**：放大的前提同难民——旧版警察局的威胁靠**几何摆位 + 固定像素感知半径**（`PoliceAggroRadius=400` 对着白昼视距 300 标定）撑着，核心护栏「**任一可行走点能感知到你的丧尸 ≤1 只**」全靠房间门洞遮挡把 4 只互相焊开 ⇒ 尺寸一动就可能让某点同时暴露两只（＝围攻断崖）。🔴 **[已改记·核实 2026-07-17·todo-sweeper 承 sweep-tests-b [HANDOFF]] 此处原引的「2 只胜率 16.6% / 3 只 0.8%」两个数已作废、故删**：它们出自**已被用户否决并回退的流血热口径（70/1.5）**，不是当前代码的行为。**真值待 `sweep-research-b` 复跑确认后再填——在那之前这里刻意不写数字**（🔴 也**别顺手填 `lanchester.md` 的 84.4%/24.1%**：那份报告生成于 `991b777`，**正是产出 born-stale goldfinger 报告的同一个 commit**，未经独立复跑验证，同样不可信）。**「围攻是断崖不是斜坡」这个定性结论不受影响**——放大后护栏「任一可行走点感知 ≤1」已实跑确认没撞断崖，且用扰动实验反证过护栏活着（把更衣室那只挪到走廊口即刻爆出 36 个"被 2 只同时感知"的位置）。🔴 **别顺手放宽护栏**（impl-police 明确报备）：`PoliceMaxConcurrentZombies` **维持 1 不动**——即便按新口径 2 只的胜率不算低，那 2 只仍要付 **1.05 处永久残缺 / 28% 惨胜率 / 20% 失血**，且败率里大部分是**昏迷倒地**（在丧尸堆里昏过去＝被吃掉）。「胜率不是成本」通则下取 1 ＝**比断崖点保守一格留裕度，是有意的**；放宽 ＝ 改关卡难度 ＝ 数值变更**须用户拍板**。Phase1（见上⑦、`ZombieActivation`）把拘留区那只改成**绑门实体的门后特殊丧尸**（撬开拘留区铁门才唤醒）、另 3 只走普通感知唤醒后，威胁触发与尺度无关 ⇒ 可放大。档位＝**小图**（journal 地图分档把警察局列在「小图·维持1~2天」、scout-door 结论亦为「警察局→小适度」）⇒ 与上述五张小图对齐 **2800×1900**（登记 `ExplorationLevelSize.Overrides[警察局]`）。落地：`ExplorationWalls` 的 `PoliceRooms`/`PoliceZombieSpots`/`PoliceCacheSpots`/`PoliceEntry`/`PoliceDeepest`/门 rect 随画布重排——建筑 bbox 从 1200×1320 铺开到 **2080×1580**（脊廊纵深 1040→1250、三间侧房进深普遍 +30%，放大出来的是**步行/搜刮工作量**）；`TestExploration.PoliceStation.cs` 两块地台随新 bbox 重排。🔴 **authored 拓扑语义一字未改**：中央脊廊（门厅→连廊→主走廊→拘留区）+ 三间侧向 loot 房、**4 丧尸各藏一房深角**、禁闭区两件甲、拘留区铁门是禁闭区**唯一入口**（普通锁 `LockTier.Standard`）、`PoliceSpotBehindHoldingDoor` 绑定关系，全部照旧——只重排坐标。🔴 **门宽 140 刻意不缩放**（＝走廊宽，门板铺满走廊穿墙口 ⇒ 走廊宽不缩放则门宽恒定；同 `ExplorationWalls.cs` 医院「门宽刻意不缩放」先例）。🔴 **不新增搜刮 id**（受"新搜刮点须用户先写叙事 draft"约束）⇒ 仍 5 点（前台/办公桌/证物柜/更衣柜/囚室）、`PoliceStationCacheTests` 计数恒绿。护栏：`PoliceStationTests` 全部重核并全绿——**核心「任一可行走点感知≤1」放大后实跑确认没撞断崖**（放大拉开丧尸间距，且已用扰动实验反证该护栏活着：把更衣室那只挪到走廊口即刻爆出 36 个"被 2 只同时感知"的位置），另"入口看不见任何一只"/"墙是房间补集不漏洞"/"从入口走得到每点和最深处"/"锁着够不到囚室·撬开才可达·唯一入口"/门板坐标断言随新画布重排；`ZombieActivationTests.Police_*` 的门后丧尸坐标同步重钉（1120,220→1960,240，语义未动）；`ExplorationLevelSizeTests` 警察局移出回退 Theory 入 `SmallMaps_Enlarged_To_2800x1900` 专测。🔴 仍待实机校准（headless 不能自证）：ⓐ放大后"一间间摸过去"的手感/搜刮点不落墙/脊廊动线只能目视；ⓑ**放大后撬开铁门那刻门后那只从拘留区深处冲出来的观感**（房间大了，它要跑一段）；ⓒ`PoliceAggroRadius=400` 的保守裕度在放大后是否仍合适（现结论是更宽松，但真实感知走引擎视野锥、只能实机核）；ⓓ叙事调查点（`NarrativeSpot.cs` 非本单）若日后给警察局加，须按新画布布点。关联 `ExplorationWalls.cs` / `TestExploration.PoliceStation.cs` / `ExplorationLevelSize.cs`。
17. **批次14~17 遗留（感染竞速 / 草药 / 小数化 / 部位细分 / 文案，规则见 §5 感染进度竞速 / §5 部位细分 / §2 数值精度通则+文风约定 / §6 开局装备）**：①**感染竞速速率待 Sim**——`r_i`（T_i≈6 天）、`CureBase`（≈67/天，抗生素 1~2 天愈）拟定待调，校准目标"抗生素从容治愈 / 草药膏须趁早才赢 / 纯茶单用赢不了但有贡献"（三档药七个百分比是用户原话、不动）；②**玫瑰果茶叠加口径已裁定=乘算**（2026-07-17 impl-effpool）——+9pp 与睡床 +10pp 可叠加，且叠加走**乘算**（1.10×1.09=1.199，见 ⑩ / `EfficiencyPoolBonusPct`）；24h 仍以"覆盖一次昼夜恢复"近似（GameClock 无细游戏小时读数），近似口径待确认；③**草药绷带非独占默认待确认**——可与针线叠加（如需严格二选一另议）；④**假肢物品化 / 损耗待确认**——假肢安装=手术、耗材=假肢本体（沿用 Grade 体系），失败时假肢是否损耗拟定待确认；⑤**伤害小数化的期望微降**（约 0.3~0.5，取整红利消失）待后续数值校准吸收；⑥**部位细分 HP 补偿复核**——躯干 28→36 是已采用方案，**备选=下调全局失血速率 `BleedRatePerWound`**（或重设更短战斗基线），供用户复核（见 §5 部位 HP 表补偿说明）；⑦**草药采集 / 种植系统另议**——草药现仅靠野外探索点散布，可持续获取路径待设计（关联 §4 食物再生同类）；⑧**文案 draft 全集待用户**——黑色幽默物品描述"待接线文案表"（含武器锚点长矛 / 土制猎枪；`flavor-text` [DECISION] 上抛的"**无描述字段+无展示 UI**"品类中，**护甲 / 狗装备已闭环**（批次18：护甲表 v2 说明列 → `ArmorLayer.Description`，`CharacterPanel` 展示）；🔴 **[已改记·核实 2026-07-17·todo-sweeper + sweep-ui]「余武器仍无描述 UI，是否给武器加待裁」= 陈旧（说没做、其实做了）——武器与护甲同形态早已闭环，无需用户裁**。铁证五点：①**字段在**——`src/DeadSignal.Combat/Model.cs:34` `Weapon.Description`，登记于 `WeaponTable.cs:463` `d[w.Name] = w.Description;`；②**回查入口在**——`WeaponTable.cs:475` `DescriptionOf(name)`；③**自动填充在**——`godot/scripts/Item.cs:80`（未显式给描述时按武器名自动填一行风味文案供库存 UI 展示）；④**展示 UI 在**——`CharacterPanel.cs:329-332`（悬停已装武器 → 原生 tooltip 显示 `weapon.Description`）+ `StashPanel.cs:424`（悬停物品行 → 底部 `_descLabel`）；⑤**文案覆盖满**——`weapons.json` 条目数 28 vs `Description` 数 28（含新增的自制猎枪 :345），`WeaponTable.cs:449-452` 明写覆盖 Arsenal 全表 + 天生武器（爪击/撕咬），骨刀/短弓已并入 Arsenal 自动收录。**仍无描述的只剩建造物/家具**（`CampStructure.cs`/`Recipe.cs` 均无 `Description` 字段），是否要给它们加＝**待裁**）、感染分档显示文案、截肢 / 假肢手术文案。**草药 / 药品 / 手术耗材描述已按用户表定稿落地（批次18，8 条整条替换）**，文风锚点同步为「现代医学的结晶」（§2）。关联 `HealthConditions.cs`(`MedicineCatalog`/`InfectionCourseLogic`) / `Silver.cs` / `HumanBody.cs` / `Materials.cs`。
18. ~~数值填写表同步流程（items-data.xlsx）~~ **[已销单·核实 2026-07-18·todo-consolidator]** xlsx 已退役，`docs/wiki/` 是唯一编辑面；后续 Wiki 改动按同步单回写代码并过门禁。设计真源已记录 [SPEC-B21]。
19. **批次18 护甲表 v2 遗留**（规则见 §6「护甲表 v2」/ §5 成对装备覆盖口径；**非 bug——"狠辣"与"取舍"本就是有意设计，勿当平衡问题擅自"修复"**）：
    - ①**数值支配现象待用户裁决**：
      - ✅ ~~皮甲 vs 皮夹克~~ **已由用户解决**（皮甲改 **18/9**）。
      - ⚠️ **但新引入了一个：皮革胸甲被皮甲支配** —— 二者同为装甲层、防护**完全相同（18/9）**，而皮甲的覆盖（胸腹双臂）是皮革胸甲（仅胸）的**超集**，胸甲唯一优势只剩**轻 2kg**。**看起来像是改皮甲时没注意撞上了胸甲的值** ⇒ 先问一句**是不是有意的**：(A) 就这样（2kg 差价买 3 个部位，负重紧时胸甲仍有位置）/ (B) 皮革胸甲下压（如 14/7）/ (C) 皮甲上抬拉开梯度（如 22/11）。
      - ⚠️ 仍在：**铁丝头甲**(12/12, 1.5kg) vs **铁皮头甲**(18/12, 3kg) 钝防持平而重量减半；**短裤** vs **长裤**（同防护、长裤多护小腿 ⇒ 短裤纯劣）；**粗布背心** vs **粗布外套**（同层同值、外套多护双臂 ⇒ 背心纯劣）。保留亦可——**若定位就是"劣质替代品"，它们本就该被支配**。
    - ②~~武器表重做中~~ **✅ 已闭环（批次19 落地终态，见 §5「武器数值终态」）**：近战锐器下限 → **1**（匕首 1~7）/ **枪不降下限**（用户："子弹没有擦破皮"）/ 爪击 **1~5** / **删土制枪 + 新增自制猎枪** / 步枪穿透 **40% + 二连发** / 冲锋枪冷却 2.6s。**布甲已复活** —— 丧尸对匕首的挡下率 0% → **~20%**，"护甲值/2 > 武器最低伤害"这条硬约束**从武器侧解决了**。
      - 🔴 **[已改记·核实 2026-07-17·todo-sweeper]「钝器提伤」这半句原写的数字已作废**：真值 **棍棒 4~8 / 尖头锤 6~14 / 破甲锤 10~18**（`weapons.json:253-254`/`:284-285`/`:315-316`），用户后来手改回调过、清单没跟。详见第 1 项。
    - ④**成对装备 UI 目视验收**（GUI，headless 不能自证）：手套 / 鞋"一件占一槽、装两件"后的角色面板呈现（同名两件并列、按槽卸下不连坐）、断手时自动落到另一侧槽的表现。

20. **批次19 遗留（战斗 / 潜行 / 后勤 / AI 四层大改；规则见 §2 设计通则 · §5 · §6 · §7）**

    **🔴 A. 机制已完整、但没有落地场景**
    - ~~①围栏升级没有入口~~ —— **已完成**（右键自家墙 → 加固 / 修补；下令按边、结算按格；**墙只能升级不能新建**，防 kill box。见 §5"围栏升级与修补"）。
    - ② **[已销单·核实 2026-07-18·todo-consolidator]** 探索关门与金手指帮门/外围围栏已落地；护栏见 `ExplorationWallsTests`/`ExplorationCacheTests`。
      - **静态遗留**：金手指帮外围围栏目前是 authored 几何与门锁接线，尚未接 `CampStructureState` 动态 HP/破坏消费；归 GUI/后续工程，不再作为“没有围栏”的开放任务。
      - ~~岗哨没有调用点 / 守备是丧尸~~ —— **已完成**（用户澄清「金手指帮是人，不是丧尸」）：**4 名**守备改为持械的 `Raider`，**杀了能扒**；其中 3 人接 `ConfigureSentry` 站岗扫视（岗哨系统首个调用点）。他们「刚经历完异常战斗」⇒ 预置伤（部位 HP 缺口 + 骨折），见 `GoldfingerGang.cs` 与 `docs/research/2026-07-14-goldfinger-calibration.md`。
        - 🔴 **[已改记·2026-07-18·用户裁决] 金手指帮根据地当次关卡固定 4 名存活帮众/守备（玩家探险队上限仍为 3，布鲁斯不占名额）**：当前配比 **2 短剑 + 2 匕首**，3 人站岗；`GoldfingerGang.Roster` 与 `Posts` 同步收束，关卡生成/纯逻辑测试已更新。机器生成的 `docs/research/2026-07-14-goldfinger-calibration.md` 保留旧 8 人数字，**待单独重跑，当前不改报告**。

    **🔴 B. 待用户拍板**
    - ③**自制猎枪现为全枪械最弱，没有任何理由去捡**（打丧尸 95.1% / 打长剑手·中甲 44.8% / 打重甲 20.1%，三项垫底）。它按"低伤高穿透 ⇒ 专克穿甲"设计，但**那个定位在本引擎里不成立**：穿透只压低防御骰上限，**能否击穿主要由伤害骰决定** ⇒ 低伤高穿透**反而更钻不动**（重甲平均穿透层数 1.13 → **0.79**、被完全挡下 46.3% → **60.6%**）。两条路：**(a)** 保规则、抬它的伤害量级（穿透才吃得上劲）；**(b)** 改规则形态——让穿透也作用于"挡下门槛"（如高穿透免疫"锐转钝"降级）。**(b) 属战斗规则变更，必须用户拍板，不自行引申。**
    - ④ **[已销单·核实 2026-07-18·todo-consolidator]** 狙击枪已固定投放于金手指帮深处军械柜，与长子弹形成真实消费链；投放位置/奖励密度仍属 Wiki 可调内容，非“有弹无枪”。
    - ⑤**商人卖不卖弹药**（玩法分叉）：当前**只收购、不出售** —— 否则"白银 → 弹药"会让"枪打不起"当场失效。
    - ⑥**弹药投放量 = 难度旋钮，请先调这个**：全图仅 **9 个子弹零件**（≈72 发短子弹 / 45 发中子弹 / 18 发长子弹）。一场冲锋枪硬仗 ≈ **27 发** ≈ 打光全地图能捡到的短子弹。**这是"强但打不起"的极致，也可能过狠。**
    - ⑦**相位中文名待用户过目**：白天筹备 / 出发路上 / 外出探索 / 返回营地 / 黄昏聚餐 / 夜间部署 / 夜间行动 / 清晨聚餐。

    **🔴 C. 已知缺口（比它们所在的那个单子更大）**
    - ⑧ **[已销单·核实 2026-07-18·todo-consolidator]** 实时潜行已消费真实服饰潜行值与负重噪音因子，并接耗子 L3 暗处视距/破隐先手；残留为 GUI 目视与数值校准。
    - ⑨ **[已销单·核实 2026-07-18·todo-consolidator]** `NoiseCue`/`NoiseCueOverlay` 已显示正半径等距噪音环，步行短/淡、动作按 `NoiseLogic` 取值；残留为 GUI 可读性目视。
    - ⑩**百分比加成的加算残留（三条已全部整改）**（§2 通则①）：~~**诺蒂读速** `1 + 自身 + 全营`~~ **[已整改·核实 2026-07-17]** 代码 `ReadingSpeed.Effective` 已全乘算 `(1+自身) × 全营乘子`，L3 = 1.50 × 1.25 = **×1.875**（护栏 `SurvivorPerksTests` L171 断言 1.875）；~~**南丁格尔全营感染率** `1 − 0.15 − 0.10`~~ **[已整改·核实 2026-07-17]** 代码 `NightingalePerk.CampInfectionMultiplier` 已乘算 `×(1−0.15)×(1−0.10)` = **×0.765**（护栏 `NurseRecruitTests` L245 断言 0.765，另有反例锁死旧加算）；~~**手术恢复效率的"效率点数池"**（睡床 +10pp / 玫瑰果茶 +9pp）~~ **[已裁定=算·已改乘算·2026-07-20]** 来源之间连乘（1.10×1.09=**1.199** → +19.9pp）；通用休养 ×1.5 已删除，床铺占比改按游戏分钟。护栏 `HealthConditionsTests.Bed_and_rosehip_tea_stack_multiplicatively_not_additively` 断言 0.6602。
    - ⑪**逃跑的劫掠者离场处置**（编排层的玩法取舍，非遗漏）：逃到地图边缘后**仍在场上、仍可被追杀** ⇒ 玩家得去补刀才结算（"它跑墙角了，你追不追？"）。若要"逃脱 = 直接消失 + 立刻结算"，机制钩子已备 —— **它同时就是"逃掉的劫掠者以后再来报复"的剧情钩子**（机制归系统，剧情归 authored）。

    **D. 目视验收（GUI，headless 不能自证；用户在工位时统一看一轮）**
    - ⑫围栏切格（100px 一格）的**视觉接缝**；⑬移动路径线（全员各一条）的**夜间可读性**与是否太吵；⑭门态徽标（四态四形状 + 锁的刻痕）在像素风 22px 见方里**认不认得出**、5 扇门常驻 5 枚牌子是否太吵；⑮右键菜单（非模态）的手感；⑯**尸体消失是瞬时的**（无腐烂渐变 / 淡出）；⑰沙袋摆放模式 + 掩体青色飘字 /「掩」标；⑱劫掠者包抄 / 找掩体 / 探头开火的观感；⑲逐件搜刮的**头顶进度条**（多人并发时几条会不会互相叠压）。

    **E. draft 文案待用户定稿**
    - ⑳精英丧尸两套样板（防暴警察丧尸 / 军人丧尸）；祖母尸体的 4 屏叙事；**耗子**的前史 / 性格 / 下水道缘由 / 关系 / 招募正文 / 气泡 / 支线 / 结局反应；**皮特**除高中生与田径队之外的前史 / 性格 / 关系 / 事件对白 / 气泡 / 支线 / 结局反应；克莉丝汀尚未写定的角色关系；~~自制猎枪~~（**[已改记·核实 2026-07-17·todo-sweeper] 自制猎枪描述已落 draft 并已在 UI 可见**：`weapons.json:345`「用水管和废铁凑出来的猎枪，准头听天由命，后坐力管够——开火之后，你和目标谁更慌还不一定。」⇒ 只待用户终审文案，非"待写"）／ 沙袋 / 子弹零件 / 两顶头盔等新物品的黑色幽默描述（**这几件仍真的没有**——建造物/材料类无 `Description` 字段，见第 17 项⑧）。

    **F. 数值皆"拟定待调"，且大半只能实机校准**
    - ㉑噪音全表（撬锁30 / 静默拆35 / 走路40 / 弓弩50~70 / 近战90~150 / 砸门180 / 枪350~700）、撬锁耗时与成功率、掩体 25% 与紧贴 24px、沙袋配方、尸体概念格 32px 与 240 具封顶、逐件搜刮 3.0s/件、**负重四档**（✅ **T45 已解决**：装备已计入负重账、debuff 已接线到移速/攻速。**三条线维持 30/50/80（用户拍板）**；惩罚曲线＝用户拍板四个数（50kg 移速/攻速各 −20%，80kg 移速 −80%/攻速 −50%，两段线性）。**代价形态＝装备吃掉搜刮余量，不是出门即罚**：重装出门 26.9kg 仍不罚，但免罚余量只剩 3.1kg ⇒ 搜两根木料就掉档；硬上限 80kg 现在**真的咬人**——穿板甲的人搬不空最大点位（住宅区 66kg，得留 12.9kg 在原地））、岗哨扫视两档、安静入侵 45s / 15s 发现窗。
    - ⚠️ **这些系统 Sim 一个字都测不了**（1v1、无距离、无走位、无多目标、不建模噪音与弹药 —— 见 §2 通则③），**只能实机校准**。

21. **存档系统遗留**（系统本体已完成，见 §9"存档与读档"：只有自动存档 / 一天两次（两顿聚餐）/ 读档随时 / 战斗中延后补存 / 轮转 6 槽 / 版本闸门）
    - ①**冷启动读档（从主菜单直接读）已完成** [2026-07-17 impl-coldload/impl-mainmenu；2026-07-18 impl-save-visual]：`CampMain._Ready` 已加冷启动分支——检测到 `CampMain.PendingColdLoadSlot`（静态请求槽）时**跳过 `ApplyStorageInitialStock` 与商人起步白银**、尾部走 `StartFromColdLoad`→`ApplySave` 灌档（复用与"游戏内就地覆盖"完全相同的路径，玩法状态全对），读失败退回新开局。默认 `null` ⇒ 分支不入 ⇒ 新开局路径逐字节不变。护栏＝`SaveRoundTripTests.读档会清空已有库存再灌档不会叠加`（锁死"再叠一份"隐患：restore 是清空后重填，先红后绿已验）。**producer 已就位**［impl-mainmenu］：新增最简主菜单 `godot/scenes/MainMenu.tscn`＋`godot/scripts/MainMenu.cs`，`project.godot` 的 `main_scene` 已改指它——「新开局」不设槽切 `CampMain.tscn`（逐字节等价旧直启），「读档」列 `SaveManager.List()`（≤6 轮转槽、两行文案、损坏/旧档置灰）→点某槽设 `PendingColdLoadSlot` 再切场景走冷启动读档。可选性判定抽成纯逻辑 `godot/scripts/MainMenuSlots.IsSelectable`，护栏＝`MainMenuSlotsTests`（4 条，先红后绿）。**相位视觉再入已就位**：`GameClock.Restore` 仍刻意不发 `OnGamePhaseChanged`（避免聚餐/健康日/关卡加载等重玩法副作用），`ApplySave` 恢复时钟后只调 `RefreshPhaseVisuals`，当帧对齐环境色与夜间遮暗开关；自然相位切换复用同一入口。护栏＝`ColdLoadPhaseVisualTests`（2 条，先红后绿）。GUI 目视验收仍归②。
    - ②**GUI 目视验收未做**（用户在工位，全程未启窗口）。待验：**主菜单长相**（`MainMenu.tscn`：标题/新开局/读档两入口）、**读档面板的两行文案**（"第 12 天 · 黄昏聚餐" / "4 人存活 · 存于 …"）与损坏/旧档置灰态、点某槽真能冷启动读该档（producer→`PendingColdLoadSlot`→切场景）、F5 游戏内读档面板；围栏升级的右键菜单成本提示、**升档后墙的外观确实变了**（TierStyle）、缺口格也能点。
    - ③`SaveCodec.PeekMeta` 仍 Parse 整个 JSON（1.6ms vs 全档 2.4ms，只快 1.5×）——够快，未做流式优化。
    - ④`_bruce` 不在场时读档**不会凭空造狗**（狗入队是 StoryFlags 驱动的剧情事件，靠 flag 重放）——这是有意的，非 bug。

---

## [T49] 废弃医院 —— 遗留与待用户

**已落地**（impl-hospital）：正名「医院→**废弃医院**」（内部路由键仍 `"医院"`）· **大地图 + 中危** · 30 处搜刮点（医疗集中于药房/手术层）· 14 只丧尸 · **楼层平面**（3 入口 / 4 分区 / 每道边界多门洞 / 5 扇可关的门）· 探索关门系统 · **危险度 `DangerTier`**（新维度，见 §4）。护栏＝`HospitalLayoutTests`（15 条）。

- **数值全部拟定待调**（归 param-calibration）：①**丧尸 14 只**是否偏高（band 12~16）；②**门洞/入口的数量与位置**——"能绕过去"当前靠**几何不变量**保证（每道边界 ≥2 门洞、且必留一个关不上的洞），但**"绕起来爽不爽"只能实机校准**；③医疗掉落量级（医院是全游戏手术/治疗的补给源，投放多少直接决定手术系统的可玩性）。
- **🔴 GUI 目视验收（headless 不能自证）**：①**关门是否真能甩掉丧尸**——丧尸绕到另一个门洞的寻路是否合理（nav 重烘焙后的路径）；②开枪把整层楼叫醒的实际效果（噪音 350~700 vs 分区跨度）；③三个入口的选择是否真的成立（员工侧门"跳过大厅直插深区"的捷径感）。
- **待用户**：①**搜刮叙事文案**仍是 draft（30 处）；②环境叙事只做了 2 处（分诊台公告 / 住院部病房）——**刻意不编造角色与前史**，若要加剧情须用户手写。
- **探索关的门是新落地的通用能力**（不只医院能用）：`ExplorationWalls.HospitalDoors` + `TestExploration.ToggleLevelDoor` + `CampMain` 的 `leveldoor` 容器。**其它探索点若要门，照此接**（规则见设计文档 §5「探索关里的门」）。
- **顺带修掉的一个洞**：`CampMain.HitContainerAt` 此前在**探索期间仍会命中营地的容器**（两边共用同一套 cartesian 坐标系）⇒ 玩家在医院里右键，可能命中营地里坐标恰好重叠的某扇门/某张床（当场"上床养病"）。现已在关内只认关内的门。
- **[大图放大·impl-map-hospital] 抬到 ≈5 天探索量级（数值全部拟定待调）**：画布 2400×1600 → **4200×2800**（均匀 ×1.75，登记在 `ExplorationLevelSize.Overrides`）；医院全部 authored 几何（`ExplorationWalls.Hospital*`：壳体/隔墙/门洞/入口）随之 ×1.75，保 authored 布局比例；另补 **6 段悬浮绕行墙**（`HospitalDetourStubs`，不跨分区边界/不封死/离线校验不压点）制造地形复杂度；搜刮点 **30 → 44**（原点缩放 + 补 14 点，医疗仍集中药房/手术层）；丧尸 **14 → 18**（补 4 深区，皆可绕）。护栏同步：`HospitalLayoutTests`（44 点 + 探针 x=1400）、`MarketHospitalCacheTests`（44 点）、`ExplorationLevelSizeTests`（医院移出回退 Theory + 专测 4200×2800）。
  - **🔴 仍待实机校准（headless 不能自证）**：①放大后"≈5 天"是否名副其实——工作量＝路径像素/95 + Σ(件数×3) + 绕路/避战，结构上抬向 ≈3000–3600s，但**真实天数只能实机测**；②补 14 点的**掉落/坐标/文案全 draft**（`Hospital*Title/Narrative` 待用户）；③补 4 丧尸与 6 段绕行墙的**手感/可绕性**只能目视；④绕行墙对 nav 重烘焙路径的实际影响。
- **[大图放大·impl-map-village] 南林村庄抬到 ≈5 天探索量级（数值全部拟定待调）**：画布 2400×1600 → **4200×2800**（登记在 `ExplorationLevelSize.Overrides`）；`TestExploration.SouthForestVillage.cs` 补**村落骨架**——6 片实体农家院墙（`AddRoomOutline`，各留门洞）+ 2 道带缺口的长院墙（`AddSolidWall`）切巷道逼绕路（"别一片开阔"，院墙落搜刮点空隙不封点位）；搜刮点 **30 → 42**（各分区加密 + 新分区·果园梯田，非医疗掉落保医院医疗例外身份）铺满全图；游荡丧尸 `VillageWanderZombieCount` **4 → 9**（区域近 3 倍，散布不成潮）。护栏同步：`VillageCacheTests`（42 点 + Completion=42）、`ExplorationLevelSizeTests`（村庄移出回退 Theory + 专测 4200×2800）。锁屋救援点/authored 叙事调查点文本**未动**（仍归用户 draft，见第 15 项②）。
  - **🔴 仍待实机校准（headless 不能自证）**：①放大后"≈5 天"是否名副其实——同医院口径，**真实天数只能实机测**（结构性把工作量往锚点堆，literal 3000–3600s 靠"隔天复访续搜"多天模型 + 用户调数补足）；②补 12 点的**掉落/坐标/文案全 draft**（`Village*Title/Narrative` 待用户）；③院墙巷道 funnel 缺口的**可绕性/手感** + 9 只游荡丧尸密度只能目视；④院墙/长墙对 nav 重烘焙路径的实际影响（搜刮点均落开阔地已结构上避免封点，仍须实机确认可达）。

---

## [wiki-sync 核查] 本轮同步的待造 & 未做（18 页逐页核查产出）

本轮"能落地的都同步 + 覆盖缺口加列 + 开局装备接线"已做并过门禁（test 3345 全绿 / Sim 基线零漂移 / godot 构建绿）。以下留待后续：

- [x] **克莉丝汀整套 perk（已造）**：巧舌如簧三级三通路全接线——ChristinePerk.cs（新，形态照 PetePerk）+ perks.json/PerkConfig 克莉丝汀段 + SurvivorPerks.IsChristine/GrantChristine + Pawn.Create 按名授予。① 相位不掉饥饿：CampMain 聚餐结算 IsChristine 分支走 ChristinePerk.ResolveHungerPhase（L1/L2=25%、L3 加算=35% 跳过本相位衰减）；② 商人折扣/卖价：MerchantTrade.Buy(buyDiscount)/SellPrice(rateOverride) + MerchantBuyList/MerchantPanel 通路，L2 在营买入 6.25% 折扣、L3 在营卖价 60→70（[Q2] 需在营存活）；③ 升级轴：L1 入队、L2 在营存活 3 天（CampMain._christineDaysInCamp，同道格羁绊天数先例）、L3 灭金手指帮（新 GoldfingerDiscovery.GangClearedFlag，据点守备全灭时 CampMain.MaybeMarkGoldfingerGangCleared 置位）。数值取 characters.json（authored·非拟定）。先红后绿：ChristinePerkTests + MerchantChristineTests + PerkConfigMigration 克莉丝汀段。
- [x] **书效果·A桶（已造）**：~~进阶木匠「消防斧攻速+8%」~~ 已造（消费层乘子 MeleeBookEffect，Actor.EffectiveAttackInterval ×1/1.08；axe 配方的 AdvancedCarpentry 解锁原样保留）；~~尖峰时刻「骨折恢复+15%」~~ 已造（FractureRecoveryBooks×1.15，仅骨折，Pawn.AdvanceHealthDay→TickDay.fractureHealSpeedMultiplier）；~~解锁「护踝鞋具」recipe~~ 已造（Recipe.cs ankle_guard，书门槛=尖峰时刻，材料皮革2+绳1）；~~弓与箭之道「弹道速度+20%」~~ 已造（[T68] Archery.cs BookFlightSpeedMult×1.2）。~~wiki 漏列野外生存指南解锁的「恐怖装甲」~~ 已补列（books.json+bundle.js，[modsync]）。
- [x] ~~**改装·待造机制**：护手挡格 50% 格挡、弩盾 正面120° 25% 无效（均只有重量、无机制）；钉子强化/锯齿剑刃「流血」（用户标"待引擎支持"）~~ 🔴 **[陈旧登记·已销单·核实 2026-07-17·sim-checkup + todo-sweeper] 三条全部真接线，不是"只有重量、无机制"**：
  - **护手挡格**：`CombatData.cs:230` + `Actor.cs:1447`。
  - **弩盾**：`Actor.cs:1423` + `Actor.cs:1425`。
  - **流血轴**：`Effects.cs:308`/`:310`/`:318`（**引擎层，`Duel` 走得到** ⇒ 用户标的"待引擎支持"已被满足）。
  - **实跑铁证**：裸棍棒 **0.00 次流血/场** → 钉子强化 **2.69 次/场**（sim-checkup 自建对照 harness）。
  - ⚠️ **由此暴露一个数值问题（已上抛，见 journal `[DECISION] sim-checkup ①`）**：sim-checkup 当时测得**「钉子强化」被「铁丝强化」支配**——实测铁丝把棍棒 vs 长剑手·中甲 从 24.2% → **39.6%**（+15.4pp），钉子只到 **24.3%**（+0.1pp ＝ 噪声）。
    - ⚠️ **[自我修正·2026-07-17·todo-sweeper] 本条原写的「二者同占棍身部位（Shaft）、互斥二选一」已被下一条推翻**（分槽已落地，钉子改占棍头 `ClubHead`）⇒ **"被支配"这个提法作废**：二者现在可以一起装，不再是二选一。**但"钉子的边际贡献≈0"这个问题独立于分槽、依然存在**（见下面重测），只是它不该再被叫作"支配"。
    - 🔴 **也别再引用本条原写的归因**（「小流血流速 0.3、丧尸 ×1/3 体质抗性 ⇒ 换不来胜率」）——impl-church 的机制探针把它推翻了：**真因是 Sim 量程，不是流血太弱**（见下）。
  - ✅ **[已同步落地·2026-07-17·impl-church] wiki→代码：钉子/铁丝改不同槽、可叠加。** 用户原话「我在 wiki 上更新过了，现在钉子和铁丝强化不占用同一个槽，可以一起安装」；wiki 部位列＝铁丝「棍棒上部」/ 钉子「棍棒顶端」。落地：`WeaponPart` **末尾追加** `ClubHead`（不插队；该枚举不进存档、config 也只存数值不存部位）、`NailStuds.Part` 由 `Shaft`→`ClubHead`、`DisplayNames` 同步 wiki 写法（Shaft→「棍棒上部」、ClubHead→「棍棒顶端」、Handle「柄」→「剑柄」）。先红后绿（新测试 `WeaponModTests.钉子与铁丝不占同一个槽_可以一起装在同一根棍棒上`，旧代码下 `Assert.NotEqual(nails.Part, wire.Part)` 当场红）。**互斥没有任何旧测试钉着**，故无"错的旧断言"要改。
  - 🔴 **重测已跑（4000 场/格·我方中甲·代价列＝胜场占比）：分槽解决了"钉子装不上"，<b>没</b>解决"钉子装上也几乎不改变战斗结果"——这是两个问题，wiki 的改动只解决了第一个。**
    - **满改装（钉+铁+缠）vs 裸棍棒**：丧尸·穿衣 胜率 99.5%→99.9%、**毫发无伤 16.8%→22.7%**、耗时 40.4→33.2s；丧尸·光身 毫发无伤 17.7%→**25.1%**、耗时 37.4→30.6s；长剑手·中甲 胜率 24.2%→**41.5%**；长剑重装·重甲 4.3%→**8.7%**。⇒ 改装买的仍是**少受伤 + 少缠斗**，不是胜率（打丧尸本来就 99%）。
    - **钉子的边际贡献（满改装 vs 铁丝+缠手对照）≈ 0**：丧尸·光身 毫发无伤 25.1%→25.1%、耗时 30.6→30.6（**完全无差**）；丧尸·穿衣 22.6%→22.7%；长剑手 40.8%→41.5%；重装 8.2%→8.7% —— **全部落在噪声内**。单装钉子 vs 裸棍棒同样是噪声（长剑手 24.2%→24.3%，与 sim-checkup 的 +0.1pp 独立复现一致）。
    - 🔴 **但钉子不是死配方，机制真的在工作**（探针）：**打在敌人身上的流血事件 裸棍棒 0.00 次/场 → 钉子 3.36 次/场**（丧尸）/ **2.62 次/场**（长剑手）——它是**钝器唯一的流血来源**（引擎流血资格要求锐器抵达）。**它兑现不了，是因为 `DuelEngine` 打到死为止：敌人失血到「重度」只有 0.0%（丧尸）/ 0.1%（长剑手）。**
    - ⇒ **「钉子换不来胜率」很可能不是钉子的问题，是 Sim 的量程问题**：小流血买的是"不肯收口的小口子"，价值在**战斗之后**（包扎/手术/感染竞速）或**敌人脱战后失血而死**，而 Duel **1v1、打到死、不建模战后**（CLAUDE.md「Sim 测不了什么」）。**拿这张表判钉子"没用"是越界的。**
  - **代价面**：满改装＝钉子*4 + 铁丝*2 + 绳子*1，工时 60+60+60＝**180 分钟**，重量 1.50→**1.68kg**（+12%，只有铁丝加重；钉子/缠手不加重）。⚠️ **「重量是改装核心代价轴」在棍棒身上不成立**——棍棒基重仅 1.5kg，+0.18kg 对负重账几乎无感 ⇒ 棍棒满改装**几乎没有代价**（只有材料与工时）。这条是否要处理＝规则形态，待用户。
  - 🔴 **仍待用户拍板（规则形态，agent 不自裁）**：钉子的战斗收益≈0 是否要处理（抬数值 / 让小流血在战后成本里真正贵起来 / 接受"它买的是战后代价、Sim 看不见"）。第 19 项①的"数值支配现象"（皮革胸甲被皮甲支配）是同一形态。
- [ ] **改装·仍待造**：~~弓臂缠手散布-4%/攻速+4%~~ 已对齐 config+catalog（攻速+4%⇒×0.96、散布×0.96，[modsync]）；枪托型「近战等同于85%攻速的某武器」是重定义、非当前显式数值模型。
- [x] **fits/数值分歧已对齐（表赢代码，[modsync]）**：防滑缠手 fits 补「骨刀」（WeaponModCatalog.cs:199）；镂空剑刃伤害 config 0.91→0.90（-10%，weaponmods.json fuller_blade）。
- [ ] **自带装备/开局武器·平衡影响待你确认**：已接线成"除道格(棍棒+墨镜)/克莉丝汀(手枪)外全员开局无武器"（早期武器改为全靠搜刮/制作）——重大早期平衡变化；克莉丝汀是全队唯一带枪者（她 gear 文本未改故保留），若要与整体基调统一需再定。此外各角色 authored「自带装备」**描述文本**仍与新接线口径有出入（wiki 那几行会持续显示为待同步文本），要不要把描述也改齐。
  - ~~野外采集 ForageLogic~~ **实为已建成**（2026-07-17 复核）：`godot/scripts/Farming.cs` 内 `ForageLogic`（4 采集点=守林人小屋林下/柴堆蘑菇×2、斯图尔特菜地/垄尾土豆×2，各带基准量；读《野外生存指南》产量 ×1.5 乘算向下取整，不读也采得到）——纯逻辑 + 消费接线 + authored 点位全齐：踏入链路已接（`CampMain.cs:5554` 路由 → `ForageLogic.Resolve` 实产、`TestExploration.cs` 铺 4 采集点[T67]），单测已覆盖（`ForageFarmingButcheryTests.cs`：基准/向导×1.5/未知id/书门槛）。scout 立项时因 grep 误判"无此文件"（ForageLogic 住在 Farming.cs 里），此条系陈旧登记，非缺口。若要扩「row-per-spot 子表」进 wiki 展示表另议。
- [ ] **墨镜视野效果待定消费口径**：用户 authored 的「墨镜 / 自制简易墨镜白天 +5% 视野」尚需确认“视野”只指视距，还是视距与锥角同时 ×1.05；不能擅自引申。皮革胸甲/皮甲/板甲的移速 −1%/−3%/−10% 已接入真实穿戴乘算链；警察局前中期汇合点与 4 只分房丧尸也已落地。
